//The IPairWise represent the interface of elements that we want to stored, 
//it is like a structure of elements that contains the information 
//that we want to display
export interface IPairWise {
    firstElement?: number;
    secondElement?: number;
    sumElements?: number;
    posFirstElement?: number;
    posSecondElement?: number;
    sumPositions?: number;
    sumTotalPositions?: number;
    
}

